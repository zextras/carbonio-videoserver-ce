_pkgver="1.4.0"
pkgbase="carbonio-videoserver"
pkgname=("${pkgbase}-ce" "${pkgbase}-confs-ce")
pkgver="1.3.10"
pkgrel="1"
pkgdesc="An open source, general purpose, WebRTC server"
arch=('x86_64')
maintainer="Zextras <packages@zextras.com>"
url="https://zextras.com"
depends__apt=(
  "carbonio-curl"
  "carbonio-ffmpeg"
  "carbonio-libnice"
  "carbonio-libopus"
  "carbonio-librabbitmq-c"
  "carbonio-libsrtp"
  "carbonio-libusrsctp"
  "carbonio-openssl"
  "carbonio-videoserver-confs-ce"
  "cdebconf"
  "debconf"
  "dialog"
  "libc6"
  "libconfig9"
  "libglib2.0-0"
  "libjansson4"
  "libmicrohttpd12"
  "libogg0"
  "libterm-readline-gnu-perl"
  "pending-setups"
  "service-discover"
  "service-discover-base"
  "zlib1g"
)
depends__ubuntu_noble=(
  "carbonio-curl"
  "carbonio-ffmpeg"
  "carbonio-libnice"
  "carbonio-libopus"
  "carbonio-librabbitmq-c"
  "carbonio-libsrtp"
  "carbonio-libusrsctp"
  "carbonio-openssl"
  "carbonio-videoserver-confs-ce"
  "cdebconf"
  "debconf"
  "dialog"
  "libc6"
  "libconfig9"
  "libglib2.0-0t64"
  "libjansson4"
  "libmicrohttpd12t64"
  "libogg0"
  "libterm-readline-gnu-perl"
  "pending-setups"
  "service-discover"
  "service-discover-base"
  "zlib1g"
)
depends__yum=(
  "carbonio-curl"
  "carbonio-ffmpeg"
  "carbonio-libnice"
  "carbonio-libopus"
  "carbonio-librabbitmq-c"
  "carbonio-libsrtp"
  "carbonio-libusrsctp"
  "carbonio-openssl"
  "carbonio-videoserver-confs-ce"
  "jansson"
  "libconfig"
  "libmicrohttpd"
  "libogg"
  "pending-setups"
  "service-discover"
  "service-discover-base"
  "zlib"
)
makedepends__apt=(
  "autoconf"
  "automake"
  "git"
  "libconfig-dev"
  "libglib2.0-dev"
  "libjansson-dev"
  "libmicrohttpd-dev"
  "libogg-dev"
  "libtool-bin"
  "zlib1g-dev"
)
makedepends__yum=(
  "autoconf"
  "automake"
  "git"
  "glib2-devel"
  "jansson-devel"
  "libconfig-devel"
  "libmicrohttpd-devel"
  "libogg-devel"
  "libtool"
  "zlib-devel"
)
section="comm"
priority="important"
source=(
  "https://github.com/meetecho/janus-gateway/archive/refs/tags/v${_pkgver}.tar.gz"
  "621-carbonio-videoserver.sh"
  "carbonio-videoserver"
  "carbonio-videoserver.hcl"
  "carbonio-videoserver.service"
  "carbonio-videoserver-sidecar.service"
  "carbonio-videoserver-sidecar-legacy.service"
  "carbonio-videoserver-configs"
  "intentions.json"
  "janus.jcfg.patch"
  "policies.json"
  "service-protocol.json"
  "carbonio-message-broker-consul-check"
)
sha256sums=(
  'ba0d588ad2500e98cdc82e3dc2051c37ba52fa4ff374d92cfcd1145eb4315596'
  'c474d6228fa89a5ddb79a5fae444efd4fee8d7a24fc5e31c5a9d60022a6556c5'
  '51908a40468390295d80c8a80cad6d899a7883466fb53028c687185f90b487d7'
  '1b5e448ae50961cd3d2cb1cf542e43752036e4a0ccf664cc0ce13fca2e67f4cf'
  '798368b51267b7be2ad7a756c4f8506dd0d3db1484ccd017e48d4e6dfeeace8f'
  'd6b6a4d72555d1931365dcfdad26c01453831dc9b1b1e9a17f4b71cdfd15cd48'
  'e9356df33318d350c0eec141b4bb2dc728efa44f031afb9349c5aaa806574379'
  '27b56ea6ead6463e6fc5f36dd594b37826a99be8cb9f5abffc1fd3f8cbc7c3e3'
  'f93b86989d642c9879e86b7b24b7144fd300c7e40e77934a2c9cc4261fed9068'
  '414825b2a9f06c66a5efc7d5516e502bccb9ff920f4728c42cb4334aecfcd6f4'
  'cbe699652a2569ac1c5b9d9dd983d8d3788013c346a5c247425e460398771455'
  '53883f4fdfc51da84691ec7ac74378dc8337fb8514ef1de66dc1a24e9b290ba1'
  'f843bce1b2b436bf017f5c3f325c7e94787e123579d18d6c4670d3968b3ff7a7'
)

_package_common() {
  cd "${srcdir}/janus-gateway-${_pkgver}"
  make DESTDIR="${pkgdir}" install

  install -Dm755 "${srcdir}/carbonio-videoserver" \
    "${pkgdir}/usr/bin/carbonio-videoserver"
  install -Dm644 "${srcdir}/carbonio-videoserver.service" \
    "${pkgdir}/usr/lib/systemd/system/carbonio-videoserver.service"
  install -Dm644 "${srcdir}/carbonio-videoserver.hcl" \
    "${pkgdir}/etc/zextras/service-discover/carbonio-videoserver.hcl"
  install -Dm644 "${srcdir}/621-carbonio-videoserver.sh" \
    "${pkgdir}/etc/zextras/pending-setups.d/621-carbonio-videoserver.sh"
  install -Dm755 "${srcdir}/carbonio-videoserver-configs" \
    "${pkgdir}/usr/bin/carbonio-videoserver-configs"
  install -Dm644 "${srcdir}/intentions.json" \
    "${pkgdir}/etc/carbonio/videoserver/service-discover/intentions.json"
  install -Dm644 "${srcdir}/policies.json" \
    "${pkgdir}/etc/carbonio/videoserver/service-discover/policies.json"
  install -Dm644 "${srcdir}/service-protocol.json" \
    "${pkgdir}/etc/carbonio/videoserver/service-discover/service-protocol.json"
  install -Dm755 "${srcdir}/carbonio-message-broker-consul-check" \
    "${pkgdir}/usr/bin/carbonio-message-broker-consul-check"
  rm -rf "${pkgdir}"/etc/janus
  rm -rf "${pkgdir}"/opt/zextras/common/share/doc
  rm -rf "${pkgdir}"/opt/zextras/common/share/man
  rm -rf "${pkgdir}"/opt/zextras/common/share/janus/demos/
}

_package_legacy() {
  install -Dm644 "${srcdir}/carbonio-videoserver-sidecar-legacy.service" \
    "${pkgdir}/lib/systemd/system/carbonio-videoserver-sidecar.service"
}

_package_systemd() {
  install -Dm644 "${srcdir}/carbonio-videoserver-sidecar.service" \
    "${pkgdir}/lib/systemd/system/carbonio-videoserver-sidecar.service"
}

_postinst_apt_common() {
  . /usr/share/debconf/confmodule

  if [ "$1" = "configure" ]; then

    # creating videoserver group if it isn't already there
    if ! getent group videoserver >/dev/null; then
      addgroup --system --force-badname --quiet videoserver
    fi

    # creating videoserver user if it isn't already there
    if ! getent passwd videoserver >/dev/null; then
      adduser --system --force-badname --quiet \
        --ingroup videoserver \
        --home /var/lib/videoserver \
        --shell /bin/false \
        videoserver
      usermod -c "videoserver WebRTC group" videoserver
    fi

    if ! test -d /var/lib/videoserver; then
      mkdir /var/lib/videoserver
      chown -R videoserver:videoserver /var/lib/videoserver
      # This directory can contain sensitive data and should not be world-readable
      chmod 0700 /var/lib/videoserver
    fi

    public_ip="$(curl -s ifconfig.me)"
    sed -i "s#nat_1_1_mapping = \"\${PUBLIC_IP_ADDRESS}\"#nat_1_1_mapping = \"$public_ip\"#" /etc/janus/janus.jcfg
  fi
}

_postinst() {
  if [ -d /run/systemd/system ]; then
    systemctl daemon-reload >/dev/null 2>&1 || :
    systemctl enable carbonio-videoserver.service >/dev/null 2>&1 || :
    systemctl enable carbonio-videoserver-sidecar.service >/dev/null 2>&1 || :
  fi
}

_postinst_message() {
  echo "======================================================"
  echo "Carbonio videoserver installed successfully!"
  echo "You must run pending-setups to configure it correctly."
  echo "======================================================"
}

build() {
  cd "${srcdir}/janus-gateway-${_pkgver}"
  patch -Np1 -i ../janus.jcfg.patch

  export CFLAGS="-I/opt/zextras/common/include -ffile-prefix-map=${srcdir}=."
  export LDFLAGS="-Wl,-rpath,/opt/zextras/common/lib \
    -Wl,-rpath,/opt/zextras/common/lib64 \
    -Wl,-rpath-link,/opt/zextras/common/lib \
    -Wl,-rpath-link,/opt/zextras/common/lib64 \
    -L/opt/zextras/common/lib \
    -L/opt/zextras/common/lib64 \
    -L/usr/lib/x86_64-linux-gnu \
    -L/usr/lib"
  export PKG_CONFIG_PATH="/opt/zextras/common/lib/pkgconfig:/opt/zextras/common/lib64/pkgconfig"

  _host_flag=""
  _build_flag=""
  if [ -n "${CROSS_COMPILE}" ]; then
    export CC="${CROSS_COMPILE}gcc"
    export CXX="${CROSS_COMPILE}g++"
    export AR="${CROSS_COMPILE}ar"
    export RANLIB="${CROSS_COMPILE}ranlib"
    _host_flag="--host=${CROSS_COMPILE_HOST}"
    _build_flag="--build=$(gcc -dumpmachine)"
    # Resolve transitive deps of libavcodec.so (libvpx, libx264) for cross-linker
    export LDFLAGS="-Wl,--copy-dt-needed-entries ${LDFLAGS}"
  fi

  ./autogen.sh
  ./configure \
    --disable-all-handlers \
    --disable-all-plugins \
    --disable-all-transports \
    --disable-docs \
    --disable-json-logger \
    --disable-turn-rest-api \
    --enable-plugin-audiobridge \
    --enable-plugin-videoroom \
    --enable-post-processing \
    --enable-rabbitmq \
    --enable-rabbitmq-event-handler \
    --enable-rest \
    --disable-websockets \
    --prefix=/opt/zextras/common \
    --sysconfdir /etc \
    ${_host_flag} ${_build_flag}

  make
}

package_carbonio-videoserver-ce() {
  _package_common
  _package_systemd
}

package_carbonio-videoserver-ce__rocky_8() {
  _package_common
  _package_legacy
}

package_carbonio-videoserver-ce__ubuntu_jammy() {
  _package_common
  _package_legacy
}

package_carbonio-videoserver-confs-ce() {
  pkgdesc="Janus WebRTC server configuration files for Carbonio CE"
  depends=()
  backup=(
    "etc/janus/janus.jcfg"
    "etc/janus/janus.eventhandler.rabbitmqevh.jcfg"
  )
  cd "${srcdir}/janus-gateway-${_pkgver}"
  make DESTDIR="${pkgdir}" configs
}

postinst__apt() {
  _postinst_apt_common "$@"
  _postinst
  _postinst_message
}

postinst__yum() {
  _postinst
  _postinst_message
}

postrm__apt() {
  if [ "$1" = "purge" ]; then
    if getent passwd videoserver >/dev/null; then
      if which deluser >/dev/null; then
        deluser --system videoserver || echo "Could not remove videoserver user."
      fi
    fi
    if getent group videoserver >/dev/null; then
      if which delgroup >/dev/null; then
        delgroup --system videoserver || echo "Could not remove videoserver group."
      fi
    fi

    rm -f /etc/carbonio/videoserver/service-discover/token
    if [ -d /run/systemd/system ]; then
      systemctl daemon-reload >/dev/null 2>&1 || :
    fi
  fi
}

postrm__yum() {
  rm -f /etc/carbonio/videoserver/service-discover/token
  if [ -d /run/systemd/system ]; then
    systemctl daemon-reload >/dev/null 2>&1 || :
  fi
}

preinst__apt() {
  if [ -d /run/systemd/system ]; then
    systemd stop videoserver.service >/dev/null || true
  fi
}

preinst__yum() {
  getent group videoserver >/dev/null || groupadd -r videoserver
  getent passwd videoserver >/dev/null \
    || useradd -r -g videoserver -d /var/lib/videoserver -s /sbin/nologin videoserver
}

prerm() {
  if [ -d /run/systemd/system ]; then
    systemctl disable --now carbonio-videoserver.service >/dev/null 2>&1 || :
    systemctl disable --now carbonio-videoserver-sidecar.service >/dev/null 2>&1 || :
  fi
}
